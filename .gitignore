# =====================================
#  Unity (5.x / 6.x-era) .gitignore
# =====================================

# ------------------------------------------------
# 1. KEEP ProjectSettings/ in version control
#    (do NOT add 'ProjectSettings/' to .gitignore)
# ------------------------------------------------

# ------------------------------------------------
# 2. KEEP Packages/ in version control
#    (do NOT ignore 'Packages/manifest.json' or 
#    'Packages/packages-lock.json')
# ------------------------------------------------

# === Unity Generated Folders ===
# These folders are generated by Unity and should not be committed.
Library/
Temp/
Obj/
Logs/
MemoryCaptures/

# === Build Artifacts ===
Build/
Builds/
# Add specific platform build folders if needed:
# Build-iOS/
# Build-Android/

# === Visual Studio / MonoDevelop Project Files ===
*.csproj
*.unityproj
*.sln
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.opendb
*.VC.db

# === Crash / Error Reports ===
crash.log*
error.log*
*.stacktrace

# === Script Debugging and Profiling ===
*.mdb
*.ilk
*.ipdb
*.iobj

# === Unity Addressable Assets Cache (if applicable) ===
# Uncomment if you’re using older versions of Addressables
# that store local cache data here:
# Assets/AddressableAssetsData/*/AssetGroups/*.json

# === OS-Specific Files ===
# macOS
.DS_Store
# Windows
Thumbs.db
Desktop.ini

# === JetBrains Rider / Resharper ===
*.DotSettings.user
*.DotSettings

# === Unity Package Manager (UPM) Cache ===
# If you see large Node modules appear in the Packages folder, ignore them here:
Packages/com.unity.package-manager-ui/node_modules/
# (But keep manifest.json / packages-lock.json in source control!)

